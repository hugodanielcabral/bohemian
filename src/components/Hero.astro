---

---

<style>
  .overlay {
    border-image: fill 1
      linear-gradient(hsla(0, 0%, 0%, 0.7), hsla(0, 0%, 0%, 0.7));
  }

  .hero {
    background-image: url("/img/hero-1.webp");
    background-size: cover;
    background-repeat: no-repeat;
    min-block-size: 40vh;
    place-content: center;
    text-align: center;
  }

  @media screen and (min-width: 640px) {
    .hero {
      min-block-size: 70vh;
    }
  }
</style>

<section class="hero overlay">
  <div class="flex flex-col items-center gap-y-4">
    <h1
      class="text-lg sm:text-xl md:text-2xl lg:text-3xl xl:text-4xl text-white"
    >
      Tu <span class="font-bold text-primary-400">pan</span> preferido <br />
      <span class="italic">siempre cerca</span>
    </h1>
    <button
      class="bg-primary-600 px-4 py-2 rounded-md text-xs sm:text-sm md:text-base lg:text-lg xl:text-xl font-oswald hover:bg-primary-500 text-secondary-500"
    >
      Comunicaté
    </button>
  </div>
</section>
